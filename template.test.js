const lib = require('./template.library.js');

test('day XX, part 1 exampleInput', () => {
  const exampleInput = ``.split("\n");

  expect(lib.part1(exampleInput)).toBe(0);
});

test('day XX, part 2 example input', () => {
  const exampleInput = ``.split("\n");

  expect(lib.part2(exampleInput)).toBe(0);
})
